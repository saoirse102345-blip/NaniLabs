services:
  - type: web
    name: aura-infra
    env: python
    buildCommand: pip install -r aura-infra/requirements.txt
    startCommand: cd aura-infra/src && uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
